# Pi sidecar build artifacts (regenerate with packages/sidecar/build.sh)
/packages/sidecar/pi-*-unknown-*
/packages/sidecar/pi-*-apple-*
/packages/sidecar/pi-*-pc-windows-*
/packages/sidecar/pi-payload/

# Credentials / secrets defense-in-depth (none are tracked today; this keeps it
# that way if the agent ever runs with cwd inside the repo, esp. the project-level
# .hoy/ dir from HOY-222). Public keys (updater pubkey in tauri.conf.json) are fine.
auth.json
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Dependencies
node_modules/

# Rust build output
target/

# Per-user Claude settings and transient state
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Playwright MCP verification artifacts
.playwright-mcp/

# design-sync (claude.ai/design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Frontend / Vite build output
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
node_modules
dist
dist-ssr
*.local
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

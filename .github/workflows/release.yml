name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest # Apple Silicon
            args: '--target aarch64-apple-darwin'
            triple: aarch64-apple-darwin
            rust_targets: aarch64-apple-darwin
          - platform: macos-latest # Intel
            args: '--target x86_64-apple-darwin'
            triple: x86_64-apple-darwin
            rust_targets: x86_64-apple-darwin
          - platform: ubuntu-22.04
            args: ''
            triple: x86_64-unknown-linux-gnu
            rust_targets: ''
          - platform: windows-latest
            args: ''
            triple: x86_64-pc-windows-msvc
            rust_targets: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: install system dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - uses: oven-sh/setup-bun@v2

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_targets }}

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri -> target

      - name: install frontend dependencies
        run: bun install

      # externalBin is validated when tauri build evaluates the config, so the
      # per-triple sidecar binary must exist first. bash runs build.sh under Git
      # Bash on the Windows runner.
      - name: build sidecar
        shell: bash
        run: bash sidecar/build.sh ${{ matrix.triple }}

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # linuxdeploy ships as an AppImage and the ubuntu runner has no FUSE2,
          # so it must extract-and-run instead of mounting. Linux-only var,
          # ignored on macOS/Windows. Required for the AppImage the Linux updater
          # consumes (deb/rpm are not auto-updatable).
          APPIMAGE_EXTRACT_AND_RUN: '1'
          # Updater signing (HOY-187). Add these as repo secrets; without them the
          # build still produces installers but no signed updater artifacts.
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Hoy Desktop ${{ github.ref_name }}'
          releaseBody: 'See the assets to download and install this version.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
